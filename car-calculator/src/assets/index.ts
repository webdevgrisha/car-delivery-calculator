import SVG_Add from "./SVG_Add";
import SVG_Email from "./SVG_Email";
import SVG_User from "./SVG_User";
import SVG_CarFax from "./SVG_CarFax";
import SVG_Autocheck from "./SVG_Autocheck";
import SVG_Sticekr from "./SVG_Sticekr";
import SVG_Clients from "./SVG_Clients";
import SVG_Calculator from "./SVG_Calculator";
import SVG_Settings from "./SVG_Settings";
import SVG_Edit from "./SVG_Edit";
import SVG_Delete from "./SVG_Delete";
import SVG_Save from "./SVG_Save";
import SVG_Cancel from "./SVG_Cancel";
import SVG_Ports from "./SVG_Ports";

export {
    SVG_Add,
    SVG_Email,
    SVG_User,
    SVG_CarFax,
    SVG_Autocheck,
    SVG_Sticekr,
    SVG_Clients,
    SVG_Calculator,
    SVG_Settings,
    SVG_Edit,
    SVG_Delete,
    SVG_Save,
    SVG_Cancel,
    SVG_Ports,
}